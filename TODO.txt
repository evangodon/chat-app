// if someone wants a task, they can assign it to themselves using the legend:
Evan => E
Andrew => A
Julian => J

DONE Improve time stamps (time from now, 2 minutes ago.... + dates)
- Typing symbol appears when person is typing
- server side validation for names so that users can't choose any name upon message-sending. Since there are no passwords, we can still use sid or whatever in cookies
DONE color-coded users in a room, assigned randomly and unique
(J|A)- better swear filter (parse for that string anywhere, and filter it)
- group subsequent messages from same user together
E- add gif/webm support
DONE consider getting setup on JIRA
-FIX unit tests
